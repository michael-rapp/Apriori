apply plugin: 'idea'
apply plugin: 'java'

repositories {
    mavenCentral()
}

dependencies {
    compile 'org.jetbrains:annotations:15+'
    compile 'org.slf4j:slf4j-api:1.7+'
    //testCompile 'ch.qos.logback:logback:1.2+'
    testCompile 'junit:junit:4+'
}